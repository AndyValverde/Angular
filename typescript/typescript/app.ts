
(()=>{

   const enviarMision = (xman : {nombre : string}) => {
    console.log(`Enviando a ${xman.nombre}`);
   }

   resregar
   const wolverine = {
       nombre : 'Logan',
       edad : 33
   }
   enviarMision(wolverine);
   regresar(wolverine);
})();  



